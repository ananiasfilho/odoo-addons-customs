<odoo>
    <data>
        <!-- SLA Filter and Group -->
        <record id="view_helpdesk_ticket_tree_inherit_sla" model="ir.ui.view">
            <field name="name">helpdesk.ticket.tree.inherit.sla</field>
            <field name="model">helpdesk.ticket</field>
            <field name="inherit_id" ref="helpdesk_mgmt.ticket_view_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="inside">
                    <field name="sla_id"/>
                    <field name="time_to_response"/>
                    <field name="time_to_resolution"/>
                </xpath>
            </field>
        </record>

        <!-- Temporarily Removed Search Filter for SLA -->
    </data>
</odoo>